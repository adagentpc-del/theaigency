# Required in production
NODE_ENV=production
PORT=8080
BASE_URL=https://vector.example.com
SESSION_SECRET=replace-with-a-long-random-secret
DATABASE_URL=postgres://user:password@postgres:5432/vectorme

# Stripe checkout (required to sell export credits)
STRIPE_SECRET_KEY=sk_live_replace_me
STRIPE_WEBHOOK_SECRET=whsec_replace_me

# Set to disable only when Postgres is local and does not require TLS.
# PGSSLMODE=disable
