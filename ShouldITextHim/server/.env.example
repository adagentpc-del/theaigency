# Copy this file to .env for local `vercel dev` testing. NEVER commit the
# real .env file — it is already excluded in .gitignore.
#
# In production, set this as an encrypted environment variable in the
# Vercel project dashboard (Project -> Settings -> Environment Variables),
# not in a committed file.
ANTHROPIC_API_KEY=sk-ant-api03-...
